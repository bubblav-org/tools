---
name: web-scrape-md
description: Scrape public URLs to markdown through BubblaV MCP tools. Use when you need low-token web content context.
---

When you need webpage content, use MCP tool `bubblav_scrape_url` with:

```json
{ "url": "$ARGUMENTS" }
```

Return a concise summary plus the markdown when requested.
